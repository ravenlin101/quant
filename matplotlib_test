import matplotlib.pyplot as plt
import pylab as pl
import numpy as np

x = range(10)  # 横轴的数据
#y = [i*i for i in x]  # 纵轴的数据
y = range(10)
plt.plot(x, y)  # 调用pylab的plot函数绘制曲线
plt.hlines(0, 0, max(x), colors = "c", linestyles = "dashed")
plt.show()
